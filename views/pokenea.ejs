<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Pokenea - <%= pokenea.nombre %></title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; background:#0f1724; color:#e6eef8; display:flex; align-items:center; justify-content:center; height:100vh; margin:0; }
    .card { background:#111827; padding:20px; border-radius:12px; box-shadow: 0 8px 30px rgba(0,0,0,0.6); width:360px; text-align:center; }
    img { max-width:100%; border-radius:8px; }
    .name { font-size:1.6rem; margin-top:10px; }
    .phrase { margin:14px 0; font-style:italic; color:#cbd5e1; }
    .small { font-size:0.85rem; color:#9aa7b5; margin-top:8px; }
    .meta { margin-top:12px; font-size:0.9rem; color:#94a3b8; }
  </style>
</head>
<body>
  <div class="card">
    <img src="<%= pokenea.imagen %>" alt="Imagen de <%= pokenea.nombre %>" />
    <div class="name"><%= pokenea.nombre %> (Altura: <%= pokenea.altura %> m)</div>
    <div class="phrase">“<%= pokenea.frase %>”</div>
    <div class="meta">Habilidad: <strong><%= pokenea.habilidad %></strong></div>
    <div class="small">ID contenedor: <code><%= containerId || 'no disponible' %></code></div>
  </div>
</body>
</html>
